# Local Environment Configuration
# Edit this file to switch between TEST and PROD environments
# After changing, restart your dev server: Ctrl+C then npm start

# ============================================
# SIMPLIFIED CONFIGURATION
# ============================================
# The base URL is the same for both environments
# TEST environment automatically appends /TEST to the URL
# PROD environment uses the base URL as-is

# Base API URL (same for both environments)
REACT_APP_API_URL=https://api.url-example.com

# ============================================
# CURRENT ENVIRONMENT: TEST
# ============================================
# To switch to PROD, change 'test' to 'production' below

REACT_APP_ENVIRONMENT=test

# ============================================
# To switch environments, just change the line above:
# - REACT_APP_ENVIRONMENT=test      → Uses BASE_URL/TEST
# - REACT_APP_ENVIRONMENT=production → Uses BASE_URL (no suffix)
# ============================================
