import React from 'react';
import codeBanner from './code_banner.png'; // Import the image file

const Banner = () => {
  return (
    <div>
      <img src={codeBanner} />
    </div>
  );
};

export default Banner;
